<?php declare(strict_types=1);

namespace Webkernel\Platform;

/**
 * HTTP middleware stack configuration (Laravel-shaped).
 * Pipeline is recorded here; not executed until an HTTP kernel exists.
 */
final class Middleware
{
}
